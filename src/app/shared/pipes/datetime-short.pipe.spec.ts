import { DatetimeShortPipe } from './datetime-short.pipe';

describe('DatetimeShortPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeShortPipe();
    expect(pipe).toBeTruthy();
  });
});
